---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ArticleCard from '../components/ArticleCard.astro';
import { fetchApi } from '../lib/api';
import type { ArticleListResponse, Category } from '../types/api';

const [articlesData, categories] = await Promise.all([
    fetchApi<ArticleListResponse>('/articles?per_page=6'),
    fetchApi<Category[]>('/categories')
]);

const articles = articlesData?.items || [];
---

<BaseLayout title="Acasă" description="Ursoaia Educational Portal - Resurse educaționale pentru elevi și studenți">
    <Header 
        title="Ursoaia Edu" 
        subtitle="Portal educațional cu resurse de învățare pentru elevi și studenți"
    />
    
    <main class="max-w-7xl mx-auto px-4 py-12">
        {/* Categories */}
        {categories && categories.length > 0 && (
            <section class="mb-12">
                <h2 class="text-2xl font-bold text-base-content mb-6">Categorii</h2>
                <div class="flex flex-wrap gap-3">
                    {categories.map((category) => (
                        <a 
                            href={`/categories/${category.slug}`}
                            class="badge badge-lg hover:scale-105 transition-transform cursor-pointer"
                            style={`background-color: ${category.color}; color: white;`}
                        >
                            {category.name}
                        </a>
                    ))}
                </div>
            </section>
        )}
        
        {/* Featured Articles */}
        {articles.length > 0 && (
            <section>
                <div class="flex justify-between items-center mb-6">
                    <h2 class="text-2xl font-bold text-base-content">Articole Recente</h2>
                    <a href="/articles" class="btn btn-ghost btn-sm text-primary">
                        Vezi toate →
                    </a>
                </div>
                
                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                    {articles.map((article) => (
                        <ArticleCard 
                            id={article.id}
                            title={article.title}
                            slug={article.slug}
                            excerpt={article.excerpt}
                            coverImage={article.cover_image}
                            publishedAt={article.published_at}
                            categories={article.categories}
                            tags={article.tags}
                            viewsCount={article.views_count}
                            readingTime={article.reading_time}
                        />
                    ))}
                </div>
            </section>
        )}
        
        {articles.length === 0 && (
            <div class="text-center py-16">
                <svg xmlns="http://www.w3.org/2000/svg" class="w-16 h-16 mx-auto text-base-content/50 mb-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z" />
                </svg>
                <h3 class="text-lg font-semibold text-base-content/60">Nu există articole</h3>
                <p class="text-base-content/50">Verificați din nou mai târziu.</p>
            </div>
        )}
    </main>
    
    <div class="max-w-7xl mx-auto px-4">
        <Footer 
            title="Începe să înveți astăzi!"
            subtitle="Explorează colecția noastră de resurse educaționale"
        />
    </div>
</BaseLayout>
